#cloud-config
package_update: true
package_upgrade: false
packages:
  - iputils-ping
  - traceroute
  - net-tools
  - dnsutils
  - curl

runcmd:
  - [ bash, -lc, "echo 'Lab 6 VPN VM ready' > /etc/motd" ]
